name: Deploy to server
on: push
jobs:
  first_jobs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hola mundo
        run: echo "Hola mundo"
  second_jobs:
    needs:
      - first_jobs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hola mundo segundo
        run: echo "nuevo saludo"
